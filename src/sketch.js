function setup() {
    var canvas = createCanvas(WIDTH, HEIGHT);
  
    // Move the canvas so it's inside our <div id="sketch-holder">.
    canvas.parent('mycanvas');
    console.log("hi")
  
    background(255, 0, 200);
  }
  function draw() {
    // var canvas = createCanvas(WIDTH, 0xfff0cf);
    console.log("hi")
    // Move the canvas so it's inside our <div id="sketch-holder">.
    // canvas.parent('sketch-holder');
  
    // background(255, 0, 200);
  }
  